spring:
  profiles:
    active: dev
server:
  port: 8201
spring:
  profile: dev
  application: 
    name: spring-cloud-config-client
content: hello dev

server:
  port: 8202
spring:
  profiles: test
  application:
    name: spring-cloud-config-client
 content: hello test2256
